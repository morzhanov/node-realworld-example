import * as React from 'react';
import styled from '@emotion/styled';

const AuthWrapper = styled.div`
  width: 100%;
  height: 100%;
`;

type Props = {};

// TODO: add elements from material ui
const Auth = ({  }: Props) => <AuthWrapper />;

export default Auth;

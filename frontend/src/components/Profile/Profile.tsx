import * as React from 'react';
import styled from '@emotion/styled';

const ProfileWrapper = styled.div`
  width: 100%;
  height: 100%;
`;

type Props = {};

// TODO: add elements from material ui
const Profile = ({  }: Props) => <ProfileWrapper />;

export default Profile;
